# Food Group Asset Library

Store food images by educational category so the game can scale without changing code structure.

- `protective`
- `energy`
- `body_building`

Each subfolder can contain transparent PNG assets, metadata files, or future animation variants.
